"use strict"

const myWeight1 = 65;
const myWeight2 = 70;
const myLength1 = 1.92;
const myLength2 = 1.69;
const myBMI1 = myWeight1 / (myLength1 * myLength1);
const myBMI2 = myWeight2 / (myLength2 * myLength2);

console.log(`De BMI van persoon 1 is ${myBMI1.toFixed(2)}`);
console.log(`De BMI van persoon 2 is ${myBMI2.toFixed(2)}`);